# Issue reproductions

Minimal notebooks reproducing reported issues — kept out of `examples/`
so they aren't mistaken for recommended usage.

| File | Issue |
|------|-------|
| [widget_reset_issue_example.py](widget_reset_issue_example.py) | [#5](https://github.com/kihaji/deckgl-marimo/issues/5) — camera resets when the Map is rebuilt in a reactive cell (the anti-pattern; `examples/02_hexagon.py` shows the fix) |
